export function Blue({ children }: { children: string }) {
  return <span className="blue">{children}</span>;
}

export function Red({ children }: { children: string }) {
  return <span className="red">{children}</span>;
}
